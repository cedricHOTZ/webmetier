{
  "permissions": {
    "allow": [
      "Bash(grep -oE '#[0-9a-fA-F]{3,6}' /tmp/aicha_home.html | sort | uniq -c | sort -rn | head -30)",
      "Read(//tmp/**)",
      "WebFetch(domain:www.domoweb.fr)",
      "Bash(pnpm add *)"
    ]
  }
}
